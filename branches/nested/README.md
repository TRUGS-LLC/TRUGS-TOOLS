# Nested branch (draft)

> **Under development.** CORE is strong enough for most cases.

See `../README.md` for the branches overview. Schema lives in `src/trugs_tools/schemas/nested.schema.json` when available.
