"""Internal maintainer utilities — NOT exposed via the `tg` CLI.

Modules here are invoked directly (e.g. `python -m trugs_tools.internal.build_language_trug`)
and are not part of the public tg command surface.
"""
